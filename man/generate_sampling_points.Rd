% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_sampling_points.R
\name{generate_sampling_points}
\alias{generate_sampling_points}
\title{Sample EucFACE Cells with Maximum Distance and Stratified by Photosynthetic Pathway}
\usage{
generate_sampling_points(
  gps_ref,
  samples_per_plot = 6,
  min_distance = 0.4,
  target_species = NULL,
  export_csv = FALSE,
  filename = "eucface_samples.csv",
  color_palette = NULL
)
}
\arguments{
\item{gps_ref}{A data frame with columns: Ring, Plot, Latitude, Longitude, Species, and Photosynthetic_Pathway.}

\item{samples_per_plot}{Number of samples to select per plot (default: 6).}

\item{min_distance}{Minimum allowed distance (in meters) between samples (default: 0.4).}

\item{target_species}{Optional character vector of species names to filter (e.g., c("centella_asiatica")).}

\item{export_csv}{Logical. If TRUE, exports sampled data to CSV (default: FALSE).}

\item{filename}{Output filename for CSV if \code{export_csv = TRUE} (default: "eucface_samples.csv").}

\item{color_palette}{Optional named vector of colors to use for species. If NULL, a default palette is generated.}
}
\value{
A list with:
\describe{
\item{samples}{A \code{data.frame} of sampled observations.}
\item{plot}{A \code{ggplot2} object showing faceted sampling layout.}
\item{map}{A \code{leaflet} map with sampling points.}
\item{moran_test}{A list of Moran's I test results per plot.}
}
}
\description{
This function selects spatially distributed and photosynthetically balanced samples
from EucFACE vegetation data. It performs distance-constrained random sampling,
ensuring each plot gets an equal number of C3 and C4 species, and computes spatial
autocorrelation using Moran's I. Output includes plots, maps, and optionally a CSV file.
}
\details{
Assumes the input coordinate reference system is WGS84 (EPSG:4326) and transforms to UTM zone 56S (EPSG:32756).
Each plot is sampled to obtain half C3 and half C4 species, if available.
}
\examples{
data(synthetic_gps)
result <- generate_sampling_points(
  gps_ref = synthetic_gps,
  samples_per_plot = 6,
  min_distance = 0.4,
  target_species = c("centella_asiatica", "fimbristylis_dichotoma")
)
print(result$plot)
result$map
head(result$samples)

}
